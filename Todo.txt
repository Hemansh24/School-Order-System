Set the system such as when the billing and the shipping address are the same, it generates a sub order no. instead of order no.

add the authentication layer, so that a login system is available

remove the linked schools tab and replace it with a name field checkbox with the option for multiple schools to be linked

import the previous orders and import them in our current structure
